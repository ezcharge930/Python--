{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Python 调试程序: 当前文件",
      "type": "debugpy",
      "request": "launch",
      "program": "${file}",
      "cwd": "${fileDirname}", // 关键：将工作目录设为脚本所在目录
      "console": "integratedTerminal"
    }
  ]
}
